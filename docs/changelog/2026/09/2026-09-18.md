# 2026-09-18

## Changed

- `[client]` 进入游戏后的浏览器标签标题改为 HappyRO，不再显示 roBrowserLegacy。
- `[client]` 浏览器标签页图标和 PWA 安装图标改为与后台相同的角色头像。
- `[gateway]` 站点根路径 `/favicon.ico` 改为与 PWA 相同的角色头像，避免浏览器标签先闪旧图标。
- `[root]` 将下一次 Docker 发布版本调整为 v0.2.1。
- `[root]` 删除旧的 v0.2.1 镜像归档和离线包后全量无缓存重建 Gateway、Server、Admin、Database，并重新生成 `happyro-v0.2.1.zip`；Mac arm64 从该 ZIP 完成空库离线导入、初始化、七服务健康检查、Gateway 启动页和后台入口验收。
- `[root]` 将同一批 v0.2.1 OCI 归档推送到 Docker Hub，并更新 `latest` 为相同内容。
