# 2026-09-03

## Added

- `[root]` 增加可复现的双语魔物资料、客户端精灵映射和 SPR 图片生成流水线。
- `[server]` 增加受认证的 Game Control HTTP 端点、map-server Unix Socket 主线程通道和能力发现。
- `[server]` 增加角色维护、魔物召唤、倍率应用与回读命令。
- `[server]` 增加 login、char、map、web 四个物理部署 systemd 服务模板。

## Documentation

- `[root]` 增加魔物资料构建命令、输入来源和输出位置说明。
- `[server]` 记录 Game Control 进程边界、生产网络约束、服务依赖和启用前置。

## Fixed

- `[root]` 根据 GRF 解压清单生成物品图片真实路径，修正官方资源名大小写不一致导致的 Linux 缺图。
- `[root]` 复用 Gateway 的 CP949 乱码恢复规则，补齐 7 个客户端物品图片资源路径。
- `[server]` 防止请求日志泄露授权凭据，并严格校验 Game Control 配置和命令 payload。
- `[server]` 防止角色多字段更新和倍率批量应用在失败时留下部分修改。
