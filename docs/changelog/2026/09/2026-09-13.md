# 2026-09-13

- 新增共用地图目录生成工具，同步游戏与后台目录、名称及分线信息，并导出缺失地图的地形预览；部署资源包增加 terrain 只读目录。后台精简顶栏、账号菜单及主题设置入口，固定简体中文。

- 客户端优化登录页注册与记住账号排版、聊天工具栏及消息过滤设置、技能树标题栏；后台离线时隐藏物品图鉴，转职成功关闭冒险工具，并交换冒险工具与签到菜单位置。完整 PWA 已更新本机验收入口。

- 冒险工具后台不可用时隐藏依赖 Admin 的页签并回退到可用页签，保留本地地图、NPC 和魔物功能。

- 准备 Gateway、Server、Admin、Database 四类镜像及无源码 Compose 部署方案；资源统一只读挂载，持久化使用宿主机 data 目录，不使用 named volume。
- 新增 tools/deployment 下的资源打包校验、密钥初始化、备份恢复和分阶段镜像构建发布工具，补齐后台生产运行、初始化、控制接口和健康检查配置；整理部署文档并移除旧入口。本次仅完成配置与离线校验，未构建或发布镜像。

- 客户端 Alt+A 属性窗口适配 32767 等大数值，保留原皮肤，调整数值列、加成分格及加点按钮；完整 PWA 已更新本机入口。

- 客户端修复背包卡片详情“查看”按钮文字溢出，保持单行居中，底部间距调整为 6px。

- 客户端修复世界地图魔物开关后的标签排布、连接线与楼层悬停目标；导航重新打开当前地图，未选点时随机传送所展示的地图。完整 PWA 已更新本机验收入口。

- 客户端修复地图图鉴跨传送点关闭的问题，恢复原窗口及选点状态，并消除恢复函数与状态变量同名导致的进入地图异常；完整 PWA 已更新，按要求未运行测试。

- 客户端进入地图时默认展开聊天输入栏；修复导航地形底图、寻路线程数据、传送点及 NPC 对话传送续行、路线分段显示，统一蓝色角色箭头与紫色目标标记，支持当前地图未选点随机传送。完整 PWA 已更新本机入口，按要求未运行测试。

- 服务端补齐伊甸园对话、任务板、教学及动态菜单漏译，修正登记地图回显和教学材料中文答案；保留脚本标识、资源名及菜单分隔符。

- 修复全量 NPC 菜单选项分隔符被汉化为全角冒号的问题，伊甸园及其他任务菜单恢复可正常选择。

- 修复 Gateway `?raw` 路径边界校验，并同步客户端 NPC 对话按钮测试与当前实现；Gateway 14 项和相关客户端测试通过。

- 客户端技能说明窗口限制高度并支持正文滚动、标题栏拖动及视口边缘避让；桌面、窄屏和超长正文浏览器验证通过，完整 PWA 已更新。

- 客户端技能说明移除时间标签右侧的“（基础）”，技能描述生成器同步更新说明文本，其他内容保持不变；5 项相关测试通过，完整 PWA 已更新至本机验收入口。

- 客户端技能说明统一展示主动技能的基础独立冷却、施放后延迟和吟唱时间；生成器区分零值与缺少服务端资料，保留等级对应关系及服务端计算实际时间的说明。完整 PWA 构建已在本机入口提供。

- 技能描述生成器从服务端技能配置生成持续时间和冷却描述；存在等级差异时逐级列出，保留重复时间对应的等级，避免去重造成歧义。同步根仓库 `skilldesctable.txt` 生成产物；客户端对应数据随客户端修复提交。

- 修复技能冷却重登显示、共享施放判断、被动技能误判；修正状态汉化负重阈值及生成技能描述的分等级时间。已在局域网 PWA 入口验证首个角色的全力推进施放和冷却提示。
- 服务端开启技能施放间隔失败提示，与客户端的失败消息处理一致。

- 客户端、Gateway、服务端共同修复游戏字节流分段/合并、背压和请求生命周期问题，保持 PACKETVER=20211103 / Renewal。
- 产品仓库各自包含对应 changelog 和回归测试；增加 `docs/operations/game-stream-reliability.md` 说明边界、测试及更新方式。
