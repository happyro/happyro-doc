# 2026-09-14

- [root] 为离线部署 Compose 服务设置固定容器名，去除默认的 `-1` 序号后缀。
- [client] 创建角色弹窗按实际尺寸动态居中，适配视口与弹窗尺寸变化；任务列表侧边标签增加选中区分，使用白色选中底色与加深灰色未选中底色。
- [root] 改为应用、资源和双架构镜像统一版本的离线包；VERSION 为唯一待发布版本来源，新增归档组装、完整校验、按 Docker 架构导入及禁止拉取的部署流程，镜像仍由另一台机器构建。
- [root] 将下一次 Docker 发布版本统一调整为 v0.2.0，同步发布规则、环境模板及部署工具示例；当前已发布版本保持 v0.1.4。
- [root] 分离镜像发布与无源码部署说明，纠正端口和宿主机目录用语，补充发布包状态、固定资料版本与升级验收步骤。
- [client] 修复冒险工具 Alt + F 快捷键，补齐已保存快捷键配置中的默认项，并在菜单提示中显示快捷键。
- [client] 将聊天选中状态边框限定到滚动锁定和窗口锁定按钮，恢复聊天框原有边框。
- [client] 修复技能树展开时左侧标签偏移，以及应用、重置按钮文案未垂直居中的问题。
- [client] 记住基本信息折叠状态，调整 HP/SP 字重和重量、Zeny 左对齐；简略模式 HP/SP 保持同一行，超长显示省略号。
- [client] 默认关闭地图雾效，保留 /fog 切换和已保存的个人设置。
- [root] 将 Docker 客户端构建阶段的 npm 固定为 11.19.1，修复 Node 22 自带 npm 10 解析 Vitest peer dependencies 时的 edgesOut 崩溃，保留完整 --all PWA 构建。
- [root] Docker 后台前端安装依赖前复制已跟踪的 .npmrc，确保 npm ci 使用与锁文件一致的项目配置。
- [root] 统一离线镜像清单与环境模板为 docker.io/happyro 完整标签，修复 Skopeo 自动补全 registry 后归档校验失败；部署继续禁止拉取镜像。
- [root] 本地镜像校验改为导出镜像并核对原始配置 SHA-256 与架构，避免 Docker containerd 返回 manifest ID 时误判，同时校验配置中绑定的全部层摘要。
- [server] 新增 game_control_allow_remote 显式开关用于隔离容器网络，默认继续限制回环地址，保留强制 Bearer 认证与密钥长度检查。
- [root] Docker Web API 显式启用隔离网络绑定；图鉴初始化命令使用 512 MB 内存，修复默认 128 MB 下初始化退出。
- [root] 发布 v0.2.0 双架构离线包，在 Mac arm64 完成离线导入、空库初始化、已有库升级、登录选角进图、后台与冒险工具、重启及备份恢复验收；amd64 运行验收尚未执行。发布镜像源码为根仓库 15197343、Server 98ad50f22。
- [root] 将 v0.2.0 镜像及离线包标签统一为 Docker Hub kugarocks/happyro-*，记录全部构建成功后推送同批双架构归档的流程。
- [admin] 初始化自动创建 admin/admin 超级管理员，并将后台账号密码最低长度改为 5 位。
- [server] 空数据库初始化自动创建 happyro/happyro GM 账号。
- [root] 更新离线部署说明，记录首次初始化自动创建 admin/admin 后台超级管理员和 happyro/happyro 游戏 GM 账号。
- [root] 移除首次部署后手动创建 admin 的旧命令，避免与自动初始化重复。
- [root] 重构项目 README，按游戏体验、冒险工具、GM 后台、架构和部署组织内容，并加入 11 张功能截图及对应说明。
