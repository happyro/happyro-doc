# 2026-09-11

## Documentation

- `[docs]` 重建文档信息架构：根 README 改为四仓库入口，详细汉化迁至 `docs/localization/`，并补齐架构、开发、运维、游戏资料导航。
- `[docs]` 将历史翻译工作区移到 `archive/translation/`，kRO 回编译输入移到 `localization/sources/kro-20211105/`，Bugfix 记录移到 `docs/history/bugfix/`。
- `[docs]` 为 `scripts/`、`tools/`、`localization/`、`inputs/`、`deploy/`、`work/`、`artifacts/` 和 `archive/` 补充目录入口说明。

## Changed

- `[client]` 技能树页签改用一转至四转的职业层级名称；世界地图以中文代表魔物、醒目等级和单一地下城标签精简魔物模式，并修正购物车购买按钮间距、重复标签、地下城悬停信息及小地图尺寸；导航窗口在选点前显示禁用操作与引导，瞬间转移前关闭导航与世界地图且不再显示成功消息，隐藏或重开窗口不再中断寻路，并暂时隐藏传送服务选项。
- `[client]` 仓库窗口改用中文标题和分类页签，排序控件统一为默认、升序、降序的冒险工具下拉样式，缩小并调整搜索与关闭按钮间距，居中打开仓库并在左侧联动物品窗口；地图切换不再自动提示无签到活动。
- `[server]` 冒险工具转换职业后自动恢复角色的 HP、SP 和 AP。
- `[server]` 合并卡普拉与 Cool Event Corp 对话中被硬拆的同一句文本，同时保留不同句子和刻意表演的换行。
- `[server]` 导航坐标传送改为静默执行，不再发送“已传送”系统消息。
- `[ops]` 服务配置刷新保留 Game Control 的认证密钥与 Socket，使冒险工具权限页签在重启后正常加载。
- `[client]` 客户端和管理后台改为消费由服务端配置生成的版本化 NPC 目录，统一启用脚本、实例 ID、中文名称、Sprite、官方导航关联和来源审计字段。
- `[client]` 冒险工具 NPC 图鉴仅保留具备传送身份的 NPC，并移除 NPC 寻路按钮。
- `[client]` 冒险工具 NPC 图鉴仅显示具备形象资源的 NPC，分离显示 Sprite 与传送 class，并补齐 31 个经审计的近坐标导航关联。
- `[admin]` 后台 NPC 查询默认与游戏内可见范围及目录顺序一致，并支持切换查看全部目录。
- `[admin]` 后台 NPC 显示范围改为下拉查询条件，默认视图精简技术审计列。
- `[client]` 客户端物品主名称及卡片前后缀统一使用完整中文目录，冒险工具补充装备洞数、合并价格并支持发放时自动鉴定。
- `[server]` Game Control 背包发放支持显式鉴定装备。
- `[gateway]` 客户端资源服务支持多个覆盖目录并优先读取已审查的运行时中文资源。
- `[client]` 卡片装备前缀改为读取中文资源表，物品名称覆盖仅保留与 ItemInfo 不同的审查项。
- `[docs]` 记录客户端本地化散装资源从生成、部署、Gateway 查找至客户端解析的完整生效链。
- `[docs]` 记录任务与地图旧版 TXT 的当前加载分支，明确暂不翻译未作为主要显示来源的文件。
- `[admin]` 后台「游戏规则」改名为「游戏设置」，页签与内容标题一致，倍率改为正数小数两列填写，修改备注改为可选。
- `[client]` 冒险工具游戏设置接口同步改为 `/game-settings`，经验倍率改为左右两列。
- `[client]` 3 洞、4 洞和无洞 Blade 的中文名称由「刀刃剑」改为「厚刃剑」。
- `[server]` Renewal 与 Pre-Renewal 物品库中 Blade、Blade_、Blade__ 的中文名称改为「厚刃剑」。
- `[admin]` 物品与魔物目录同步「厚刃剑」名称。
- `[client]` 物品 714 中文名称由「黄金徽章」改为「华丽金属」。
- `[server]` Renewal 与 Pre-Renewal 物品库中 Emperium 的中文名称改为「华丽金属」。
- `[admin]` 物品与魔物目录同步「华丽金属」名称。
- `[client]` 公会会长可再次选择标志文件；公告页确认按钮始终显示，且「确定」居中。
- `[client]` 公会职位默认英文名显示为会长、职位 N、新成员；成员职业改用中文职业名。
- `[client]` 角色头顶公会职位同步显示中文；卡片插卡窗口的确定与取消按钮间距为 2px；卡片查看按钮改到左下角。
- `[client]` 解散公会按钮及解散提示的确定按钮改用与公会公告确定相同的样式。
- `[client]` 创建公会、提示框、NPC 对话与菜单、插卡/选择、交易等界面的文字按钮统一为灰绿确定样式，成对确定/取消间距为 2px。
- `[client]` 小地图 `-` 右侧按钮改为循环切换不透明、半透明和隐藏地图，按钮栏保持可点，与 Ctrl+Tab 相同。
- `[server]` 新建公会的默认职位名改为会长、职位 1–18、新成员。
