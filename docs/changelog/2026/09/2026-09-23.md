# 2026-09-23

## Fixed

- `[client]` mobile-ui 同步 main 的 WebSocket 连接生命周期修复，解决登录切换到选角后旧连接延迟事件误报“服务器连接失败”的问题，并补充回归测试。

- `[client]` 手机登录、选角和创建角色支持横竖屏、软键盘可视区域与状态保留；进入游戏前等待横屏，游戏中竖屏显示遮罩，返回认证界面后解除方向限制。
- `[client]` 移除手机登录页退出按钮，统一注册提示与登录页的配色、圆角和触摸按钮样式。
- `[client]` 修复触屏点击注册后按钮持续高亮，仅对支持鼠标悬停的设备启用悬停样式。
- `[client]` 移动端使用独立登录背景、资源／地图加载进度页及服务器连接等待动画，清理失败路径上的等待界面。
- `[client]` 选角、创建角色及横屏提示统一为浅色；移动端锁定外层页面滚动和缩放，输入失焦后清除键盘偏移，保留内部原生滚动和游戏手势，避免页面错位后必须刷新。
- `[client]` 修正移动端启动加载页内边距导致的居中偏移，适当放大选角人物预览，并将右上角返回改为有背景和边框的按钮。
- `[client]` 恢复选角卡片原有尺寸，仅视觉放大人物预览，不再撑高角色边框或移动名称。
- `[client]` 移除移动端选角的预约删除、取消预约和确认删除入口并禁用删除操作，角色删除仅在桌面端提供。
- `[client]` 移动端竖屏选角属性最后一行统一去除底部分隔线，下方属性面板向上扩展 4px，底部操作区位置保持不变。
- `[client]` 竖屏选角改为实际给下方属性面板增加 4px 布局高度，同时缩减上方列表，替换负外边距调整。
- `[client]` 竖屏选角下方属性面板再增高 4px，累计增加 8px，上方角色列表相应缩减。
- `[client]` 移动端选角属性面板增加顶部分隔线，空角色卡使用加号与创建提示；统一创建页返回按钮及种族卡片高度，发型和发色改为随种族、性别与外观同步的真实精灵预览。
- `[client]` 移动端改用桌面独立发型图标及纯色色块，移除选项人物预览；属性顶部分隔线贴合列表底部边界，空角色提示按角色槽数据同步，避免已有人物仍显示创建提示。
- `[client]` 恢复移动端选角属性面板顶部分隔线上方原有的 12px 留白。
- `[client]` 统一竖屏选角最后一行属性与下方分隔线的间距，底线归属属性区域，移除属性与操作区之间的额外空隙。
- `[client]` 选角返回登录的确认框改用移动端浅色原生模态框，支持确认、取消、焦点限制和横竖屏布局，并隔离底层选角快捷键。
- `[client]` 在公共 UIManager 统一移动端消息、确认和错误弹窗，密码错误等提示自动使用浅色模态框；确认按钮统一为“确定”，初始焦点置于标题，重新登录时默认选中首个角色位。
- `[client]` 公共移动端弹窗隔离全局游戏触摸事件，修复密码错误提示点击确定无效；页面清理时同时移除移动端弹窗。

- [client] 增加默认隐藏的右侧中部前端日志侧栏，通过 URL debug=1 开关启用，在应用启动前捕获错误、控制台、HTTP 资源、WebSocket 元数据、界面及视口变化，每 5 秒记录状态；支持复制、下载及手动快照，隐藏输入值，不记录请求或封包正文，不改变连接时序。

- [client] 修复局域网 HTTP 下日志按钮仅选中文本而未复制的问题，剪贴板 API 不可用时在点击事件内同步复制全文，并反馈复制结果。

- [client] 修复角色预览头饰重影和相同外观角色闪烁：2D 精灵缓存按目标画布与精灵帧隔离，外观不变时复用，换色时生成新图而不覆盖旧图；移动端选角人物下移 8px，缩小与名称的间距。

- [client] 新增独立移动端横屏游戏主界面骨架：实时人物与状态信息、地图网格及位置、菜单和聊天摘要／输入；预留摇杆、攻击与技能槽位，尚未接入操作。隔离界面事件并清理换图、退出及断线生命周期，保留桌面组件装配与实体手柄支持。

- [client] 接入移动端固定摇杆、独立双指攻击、点选目标与交谈／拾取／传送入口，提供镜头调整按钮；复用现有战斗校验并处理触点取消、换图与迟到攻击响应，保留桌面交互和实体手柄路径。技能槽位仍为占位。

- [client] 接入移动快捷槽位真实图标、等级／数量、冷却与可用状态，支持三槽翻页、点选配置／替换／清空及服务端保存；复用物品和技能业务，支持自身、目标与地面施法及取消，保持摇杆触点独立和桌面默认行为。

- [client] 接入移动端横屏背包分类、物品详情、点击使用、穿戴／卸下和确认设置快捷槽，复用服务端物品状态与业务；统一非堆叠装备数量判断，隔离面板触控并避免旧摇杆触点释放误关面板，保留桌面交互。

- [client] 新增独立移动装备面板，按普通／时装／影子部位查看、卸下和从背包更换装备，支持左右部位选择及多部位占用展示；人物主要属性直接随服务器回包更新，按角色隔离，保留桌面属性和穿脱交互。

- 客户端移动游戏 UI 补齐技能查看／学习与快捷绑定、NPC 对话／输入、普通及点数／限量／材料兑换商店、背包排序／丢弃、仓库／手推车点选存取。操作复用现有封包业务并校验实时状态；修复旧 NPC 收尾覆盖仓库及移动容器旧数据残留，保留桌面交互。

- [client] 新增移动任务列表、分类、目标进度、奖励与启用切换；任务地点可打开地图，支持世界区域、地图搜索、真实地图图片和当前地图位置查看。
- [client] 聊天支持频道、私聊和消息筛选，移动私聊不再受桌面一对一弹窗偏好影响；新增好友、队伍、公会管理，支持职位权限／税率、公告、关系、公会技能学习与快捷绑定、徽章上传，操作前核对当前会话与权限。
- [client] 传送、鉴定、自动技能选择、箭矢／料理／锻造、材料转换、修理、手推车外观和怪物识别接入独立移动面板，沿用原业务封包与取消协议。
- [client] 新增移动卡片镶嵌、精炼、评级、随机／指定／升级／重置附魔、Laphine 合成／升级、装备改造和装备方案；确认前重新校验物品、材料与费用，附魔回包后结束会话并显示结果。

- 新增移动玩家交易面板：点选发起、数量与金额、双方锁定、再次确认成交及取消，按服务器回包处理背包预留与退回。
- 新增移动售卖／收购摊设置、订单核对、我的摊位与库存记录、关闭摊位，以及访问玩家摊位购买／出售；补齐 20211103 收购更新与成交删物品协议处理，保留桌面入口。
- 修复移动交易／开店面板在物品图标异步加载后重置数量和单价输入的问题。
- 移动页面外层改为禁止程序滚动的裁切区域，修复 WebKit 聚焦游戏输入框后整个 HUD 横向偏移；内部面板保持原生滚动。

- 移动银行加入金额上限、存取二次确认、等待回包防重复提交和服务端余额同步；修正银行查询封包 64 位余额的读取位置。
- 移动菜单加入独立画面／声音设置，支持草稿、保存、取消与恢复默认；复用现有偏好和实时渲染参数，不修改桌面窗口布局。

- 移动邮件使用独立横屏触控面板，支持分类搜索、分页、阅读、领取、删除确认、收件人校验、点选附件与发送核对；新邮件从菜单提示。补齐旧 NPC 邮箱入口的移动分流，保留桌面回调。
- 邮件请求保留完整 64 位邮件 ID，中文标题和正文按 UTF-8 字节计长，避免高位丢失与重复扩容。

- 移动宠物面板接入服务器状态、喂食、表演、改名、收回、饰品卸下和自动喂食设置；进化展示并重新核验亲密度与材料，保留二次确认和回包状态。捕捉改为点选可见魔物，孵化改为点选宠物蛋，桌面选择器和捕捉动画保持原分流。

- 修正生命体与佣兵技能施法者和射程来源，地面技能识别完整伴侣技能范围并使用对应实体移动；伴侣离场后的技能请求不再访问失效实体。

- 生命体／佣兵追近技能改由各自移动结束触发并重验生命周期，避免玩家移动误触发；快捷栏支持完整伴侣技能范围，移动端按实际施法者检查 SP、选择自身并处理伴侣更换。

- 新增移动生命体／佣兵独立信息面板，接入服务器状态、喂食／改名／解除／返回身边确认、攻击模式和生命体自动喂食及技能学习；移动回包不再挂载桌面信息窗口。

- 移动导航加入目的地搜索与路线预览初版；按用户要求暂缓后续实现，自动行走／停止与浏览器验收尚未完成。

- `[client]` 缩小移动游戏 HUD、菜单及背包等功能面板按钮与字号，技能快捷槽改为小圆角正方形；左上人物信息改为网格对齐，菜单与状态按钮置于下方并保留 12px 间距，菜单位于状态按钮左侧，地图独立放在右上角。
- `[client]` 调整横屏旋转提示与返回按钮的整体垂直居中；暂时隐藏聊天预览和菜单聊天入口，保留聊天实现。
