# 2026-09-16

## Changed

- `[admin]` 游戏设置隐藏配置文件来源提示，保留数值范围。

- `[client]` 冒险工具掉落倍率改为普通魔物、Mini、MVP 三列，按五类物品逐行设置。
- `[server]` 补齐 Mini 恢复品、消耗品和装备掉落倍率的 Game Control 支持及即时重载。
- `[admin]` 后台同步使用三类掉落倍率表格并补齐接口字段。

- `[root]` 使用最新源码重新发布 v0.2.0 双架构镜像和统一离线包，并完成 Mac arm64 空库安装、游戏链路、后台接口及整栈重建验收；确认可选导入文件缺失和名称超长日志均已消除。
- `[root]` 离线部署模板补充 `SOCKET_PROXY_URL` 并传入 Gateway，支持将浏览器游戏 WebSocket 指向独立源站域名，同时保持默认同源行为。
- `[root]` Admin 镜像补齐 `php.ini`，把此前依赖 PHP 内置默认值的 `memory_limit` 显式设为 256M 并配置 OPcache；nginx 开启 gzip，覆盖 JSON 接口和前端静态资源。
- `[root]` NPC 目录不再生成客户端精简副本，地图目录和 NPC 实例名称表改为写入客户端 PWA 数据目录，供运行时按需获取。
- `[client]` NPC 图鉴和地图图鉴改为向 Admin 冒险工具接口按页取数，不再把 3.6 MB 的 NPC 目录打进主包；地图详情按地图单独取回该图全部 NPC 用于预览标记。
- `[client]` 魔物图鉴掉落资料拆分为按需加载的独立文件，导航用的 NPC 实例名称表和地图目录同样改为按需获取；主包 `Online.js` 从 20.06 MB 降到 14.24 MB。
- `[root]` 更新游戏资料、架构和运维文档，同步 NPC/地图查询改为服务端分页、魔物图鉴维持客户端全量加载的架构决策，并补充 Admin 数据流调用链；清理三个已废弃的空文档目录，新增一批部署缺陷记录，移除 Docker 发布文档中已过期的 v0.2.0 具体构建记录。

## Fixed

- `[client]` 冒险工具重新打开窗口或刷新权限时不再重新挂载当前标签页，修复同一份资料被重复请求的问题。
- `[root]` Admin 初始化流程新增 NPC 目录导入命令，修复全新部署或从旧版本升级后 `game_npcs` 表为空、NPC 查询和 NPC 图鉴返回 0 条的问题。
- `[client]` 地图图鉴移除范围筛选，置顶并标注当前地图；首次打开选中当前地图，切图完成后清空搜索、回到第一页顶部并同步地图和 NPC 详情，同地图移动和传送保留浏览状态（7faa2a05）。
- `[client]` 创建角色时去除名字首尾空白，空名字明确提示“请输入角色名”；新增单字中文、英文和空白名字回归测试（79b0ba9e）。
- `[server]` 角色名最小长度设为 1 字节，允许单字中文和英文，保留空名字、重名和非法字符校验（c7fd7f2ef）。
- `[client]` 增大技能树竖向页签高度和间距（faa541c6）；更新导航相关断言并实际验证传送发起与未发起时的窗口行为（bcacf021）。
- `[admin]` 地图接口按当前地图排序后分页，隐藏分线时按主地图匹配置顶与筛选（b0d788a）。
- `[client]` 修复冒险工具窗口复用导致权限按钮和实时表单过期的问题，NPC 图鉴随切图刷新，搜索／筛选清除旧选择并重新校验可用性；掉落资料加载失败可重试。
- `[admin]` 恢复冒险工具 NPC 编号、导航原名、别名搜索及当前地图优先，地图恢复搜索相关性、图片和类别排序后分页。
- `[client]` 地图图鉴列表不再在当前地图名称后追加「当前所在」后缀。
- `[admin]` 游戏设置倍率整数不再显示小数点；超出允许范围时提示并阻止提交；保存后停留在当前标签页。

## Removed

- `[admin]` 删除游戏设置备注及配置版本表对应字段；后台与冒险工具保存均不再传递或写入备注。
