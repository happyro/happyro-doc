# 2026-09-17

## Fixed

- `[admin]` 魔物图鉴详情的掉落物品改为显示物品中文名，不再只展示 Aegis 名。
- `[admin]` 背包发放在角色离线失败后，角色重新上线可再次发放，不再沿用上次离线错误。
- `[admin]` 背包发放给离线角色时提示「角色不在线。」，不再显示 Response status:409。
- `[admin]` 后台接口错误文案统一返回中文。

## Changed

- `[client]` 移除冒险工具掉落倍率表头，并统一操作成功提示绿色样式。
- `[admin]` 经验倍率上下排列，范围改为空值提示，移除开关范围及重置按钮。
- `[client]` 冒险工具普通及成功反馈改为 Toast，错误在按钮旁展示；转职保持窗口打开，物品发放成功复用游戏提示。
- `[client]` 各页签保留浏览状态，未保存输入切换页签时保留、关闭时丢弃；内容切换时滚动归位，翻页默认选中首项，寻路禁用原因改为悬停提示。
- `[client]` 魔物筛选分为全部、普通、Mini、MVP；魔物与 NPC 图鉴默认勾选当前地图并保持用户选择，空列表提供与恢复状态同样式的查看全部按钮。
- `[client]` 补齐官方魔物精灵映射，修复国王波利、巨大黄金波利及其他较新魔物在冒险工具图鉴中没有图片的问题。
- `[admin]` 补齐官方魔物精灵映射并重新生成图片资源，修复国王波利、巨大黄金波利及其他较新魔物在后台图鉴中没有图片的问题。
- `[client]` 冒险工具魔物图鉴按魔物 ID 列出全部匹配结果，不再合并同名变体。
- `[admin]` 魔物图鉴级别筛选改为全部、普通、Mini、MVP，与冒险工具使用同一套分类。
- `[client]` 魔物图鉴用 `kind` 区分普通、Mini 和 MVP，筛选与标签不再组合 `boss` / `mvp` 布尔。
- `[client]` 冒险工具图鉴回归测试改为断言当前地图筛选由用户勾选控制，空列表不再自动取消限制。
- `[client]` 魔物与 NPC 图鉴列表计数注明当前地图或全部地图。
- `[admin]` 魔物目录以 `kind` 存储普通、Mini 和 MVP，查询与详情直接使用该字段。
- `[admin]` 物品发放页背包说明改为仅提示需在线角色，并与下方按钮拉开间距。
