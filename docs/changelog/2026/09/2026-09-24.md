# 2026-09-24

- `[client]` 移动端右下角改为五枚水平正方形快捷槽，支持八页切换并映射原有 36 槽；同步背包和技能面板绑定位置，移除顶部选中信息和旧长按攻击按钮，上下文交互移到右下角。
- `[client]` 新增附近自动战斗：选择全部魔物或指定种类，击败后继续寻找附近同类，无目标时等待；支持点选场景魔物确定种类，并跳过无法接近的目标。
- `[client]` 自动攻击技能独立配置，随机释放当前 SP 和冷却条件满足的已学习攻击／地面技能，默认及全部配置技能不可用时使用普通攻击。手动操作、打开面板、死亡、切图、断线或失焦会停止自动战斗。
- `[client]` 指向性快捷技能改为先点技能、再点目标释放，重复点同一技能取消选择；保留自身技能和地面选点流程。
- `[client]` 相关 12 个测试文件共 60 项通过，PWA 已按 `--all` 重建；实机与服务器实战由用户验收。全地图巡逻尚未实现。

- `[client]` 优化移动端四行战斗区的状态对齐、长文字省略和卡片圆角，突出自动战斗开关及停止状态。
- `[client]` 点击地面物品直接拾取，远处物品沿用走近后拾取；点击普通 NPC 直接发起交谈，移除二次拾取／交谈按钮。待选技能时点击物品或 NPC 会取消技能选择并执行对应交互。

- `[client]` 统一 PWA 入口和游戏页的全屏视口设置；移动及已安装应用在当前窗口进入游戏，启动页随可用高度滚动并避让系统安全区。

- `[client]` 修正横屏登录输入框溢出与操作区对齐：登录在上、注册在下，按钮组相对卡片垂直居中；竖屏保留单栏。

- `[client]` 修正移动镜头拉近／拉远方向，移动端室内外默认距离拉近一档；重置采用新默认，保留用户已保存距离。

- `[client]` 快捷栏改为每页六槽，上方三行对齐右侧四槽；取消施法就近替换配置行并保持等高，优化 HP／SP、地图与状态布局，隐藏移动端鼠标及摇杆画面，保留独立的拖动移动和技能指针。
- `[client]` 槽位配置分离候选列表与等级编辑，清空使用行内确认，保存后保持面板；自动战斗配置分栏、多选魔物种类，保存后关闭，删除场景添加入口。魔物多选按原始触点锁定卡片，滑动不触发勾选。
- `[client]` 普通点魔物按配置技能或普攻持续攻击该个体，结束后停止；自动战斗中点击临时切换目标，之后恢复原种类策略。手动移动可停止攻击。
- `[client]` 指向性技能走近后重新检查移动目标的位置与射程，必要时继续接近；取消过期目标和被新动作替换的请求。
- `[client]` PWA 使用 `--all` 完整重建并确认 Gateway 提供新产物；按用户要求未追加实机验收。

- `[client]` 自动战斗在手动移动期间暂停，停止移动后从当前位置继续搜怪；新增搜怪与活动范围配置，范围设置位于魔物列表之后，删除列表末尾冗余说明。
- `[client]` 自动战斗的魔物种类、技能与范围按服务器／账号／角色保存到本机，下次进入恢复配置但不自动开启战斗；保存失败时保留面板并提示。

- `[client]` 横屏选角列表改为两列并收窄属性区；创建角色页分为种族、人物与外观三列，名称置于中间人物上方，顶部返回与底部创建按钮固定，外观选项独立滚动，竖屏布局保持不变。

- `[client]` 横屏创建角色预览的旋转按钮移至人物左右两侧，减少靠近创建按钮的误触；人物预览宽度由 52px 调整为 65px，并随可用高度缩放。

- `[client]` 更新 PWA 安装图标及网页标签图标，安装图标统一为 192／512 像素；标签图标改用 PNG，同步入口与构建复制路径，保留替换前图标于 icon-src。
- `[client]` 启动器在窄竖屏下改为单列等宽按钮，缩小标题与间距；内容超过可用高度时允许滚动。
- `[client]` 移动端启动、连接与过图加载画面使用 Poring 图片，统一缩至 80px；移除加载画面中间的 HappyRO 文案，启动器首屏保留原有标题。
- `[root]` 固化跨机器资源归档脚本及文档，打包 kRO 运行客户端、物品和魔物图片，并生成同名 SHA-256 校验文件；资源归档仅供构建机传输。

- `[root]` 全量无缓存重建 v0.3.0 双架构 Gateway、Server、Admin 与 Database，组装离线包 `happyro-v0.3.0.zip`；Mac arm64 从该包完成空库安装验收，并将同一批镜像推送到 Docker Hub 的 `v0.3.0` 与 `latest`。发布记录补入各架构归档 SHA-256 与镜像 ID。
