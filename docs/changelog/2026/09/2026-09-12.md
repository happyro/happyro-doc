# 2026-09-12

## Changed

- `[client]` 导航窗口打开后置顶，拖到左上角时不再吸附到角色信息栏下方。
- `[client]` 技能树一转至四转页签增加高度和间距，避免竖排文字贴在一起。
- `[client]` 成就左侧页签选中后改用下层纯色，不再叠加上层贴图挡住文案。
- `[client]` NPC 购买购物车的购买与取消按钮间距改为 2px。
- `[client]` 银行存款、取款和金额按钮按文案加宽，避免「+1000万」超出按钮。
- `[client]` 登录窗口的记住账号勾选改到左侧。
- `[client]` 提示框底部按钮上移 1px。
- `[client]` 物品图鉴筛选改为「子类」：武器按种类，防具和卡片按穿戴部位；发放按钮统一为「发放到背包」。
- `[client]` 地图图鉴详情在地图右侧列出该图 NPC，选中后用底部「传送到这里」复用 NPC 传送。
- `[client]` 地图图鉴右侧列表标题改为「NPC」。
- `[client]` NPC 图鉴右侧资料边框不再溢出到左边地图。
- `[client]` 魔物图鉴和 NPC 图鉴用顶部「当前地图」勾选，不勾则为全世界；当前图没有魔物时默认不勾。
- `[client]` 冒险工具默认打开地图图鉴；魔物和物品图鉴默认选中第一项；物品价格标题改为「买 / 卖」。

## Fixed

- `[root]` 数据库、网关、服务端等脚本无参数时只打印帮助并退出 0；`configure-*` 需显式 `apply`，已有网关 `.env` 不再覆盖。
- `[root]` `upstreams.sh status` 只读本地 ahead/behind，不再 fetch。
- `[root]` 本地化扫描无参数只打印帮助；NPC 目录生成器将 `DISABLED` 标为非动态且不入目录。
- `[root]` 记录全仓库审查项 1–41 的独立修复说明，见 `docs/history/bugfix/zh-cn/20260912-01/`。
- `[client]` 登录框按下鼠标时不再清空账号密码；注册按钮改为独立 id，去掉 BMP 上的叠字。
- `[client]` 银行存款金额按钮对 MAX 与数字都返回有效值，金额框按 Enter 发起存款。
- `[client]` 成就「已完成 / 未完成」改为真正筛选；技能窗再次打开不再丢掉未确认加点。
- `[client]` 世界地图已打开且被挡住时快捷键置顶，去掉无效传送服务开关；`/mapmove` 改发静默地图传送包。
- `[client]` 冒险工具恢复窗口焦点，bootstrap 失败时禁止物品发放，重复点击当前页签不再整页重挂。
- `[server]` `@mapmove` 成功后不再发送英文 `Warped.`；转职或回满对死亡角色走复活流程。
- `[server]` 不可堆叠物品发放中途失败时回滚已进包格子；毁灭之剑任务中的 Emperium 改为「华丽金属」。
- `[client]` 物品图鉴防具子类不再列出卡片专用的「武器」和「任意部位」。
- `[client]` 物品说明解析 NAVI 和 ITEM 标记；详情窗口可点开导航，图鉴只显示地点名。
- `[client]` 地图图鉴按 GAT 比例裁掉官方小地图黑边再标注；选中 NPC 和点击位置都标蓝点；寻路只画未走过的路段；角色位置用导航窗口同一支箭头。
- `[client]` 寻路完成或传送成功后去掉地图标注；导航窗口同步裁边、蓝点和剩余路径，并重新显示寻路与传送按钮。
- `[client]` NPC 图鉴再次打开时按当前地图重筛，不再停留在换图前的 NPC。
- `[client]` 从官方 stateiconinfo.lub 抽出全部 720 条状态说明并换成中文，加载官方 Lua 后仍用中文覆盖。
- `[client]` 状态说明覆盖层会写入 haveTimeLimit 为 0 的条目，不再把无时限状态留成有时限。
- `[client]` NPC 图鉴右侧资料去掉与标题底边重叠的顶框，避免看起来像两条边。
