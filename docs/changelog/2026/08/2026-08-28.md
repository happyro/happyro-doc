# 2026-08-28

## Fixed

- `[client]` 修复快速注册时“记住账号”保存 `_M` 或 `_F` 注册后缀的问题，登录请求保留注册后缀，本地仅记住实际账号名。
- `[client]` 将五张人类出生客船地图统一为“沉船”，并将五张船外地图统一为“孤岛”，确保进入提示与角色列表 MAP 名称一致。
- `[server]` 将沉船内受伤 NPC 的对话标题统一为“受伤的剑士”，并同步相关剧情中的“孤岛”名称。
- `[server]` 允许角色、队伍和公会使用中文名称，并防止名称规范化破坏 UTF-8 字符。
- `[client]` 移除 api.html 对不存在的 Config.local.js 的请求，避免控制台产生无效 404。

## Added

- `[root]` 支持通过 Compose 分别配置游戏资源地址和 WebSocket 代理地址，以便接入 CDN 或 ESA。
