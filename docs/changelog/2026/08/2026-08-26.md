# 2026-08-26

## Added

- `[client]` 增加普隆德拉 NPC 全量名称表，覆盖城区、室内、教堂、王宫任务区域、图书馆、监狱、竞技场和公会城。
- `[client]` 增加第二阶段八座主要城市的 877 项显式 NPC 中文名称映射。
- `[client]` 增加 P0/P1 四组常用野外地图的 98 项显式 NPC 中文名称映射。
- `[client]` 增加 P2 妙勒尼山脉 12 张地图的 15 项显式 NPC 中文名称映射。
- `[client]` 增加 P2 朱诺周边 12 张地图的 19 项显式 NPC 中文名称映射。
- `[client]` 在 NPC 实体创建包和身份包路径统一应用中文名称，保持悬停名称、名称牌和对话标题一致。
- `[root]` 建立核心城镇 NPC 名称汉化计划、范围、术语表和验收文档。
- `[root]` 建立第二阶段八城 1,209 条活动 NPC 清单、地图范围和代表性实机验收文档。
- `[root]` 建立非城镇 NPC 名称翻译计划和优先级进度表，以普隆德拉周边野外地图为最高优先级。
- `[root]` 完成 P0/P1 共 60 张野外地图的 157 条可见 NPC 实例清单和验收文档。
- `[root]` 完成 P2 妙勒尼山脉 19 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P2 朱诺周边 36 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P2 其余六个城市周边 49 张地图、104 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 核心城市地下城 19 张地图、17 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 沿海地下城与交通区域 9 张地图、16 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 艾尔帕兰钟楼地上与地下 8 张地图、18 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 早中期地下城 7 张地图、6 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 格拉斯特海姆经典区域 17 张地图、38 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 经典地下城 12 张地图、4 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 龟岛 6 张地图、26 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 区域型地下城 20 张地图、47 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 中后期固定地下城 32 张地图、64 条可见 NPC 实例清单和批次验收文档。
- `[root]` 完成 P3 其余常用固定地图 14 张地图、58 条可见 NPC 实例清单和批次验收文档。

## Changed

- `[root]` 将长期中文产品分支迁移为 `main`，中文演示分支迁移为 `demo`。
- `[client]` 汉化商城窗口的分类、搜索、充值、购买及结果提示文本；右上角入口继续使用官方 `nc_cashshop.bmp` 图片。
- `[root]` 更正 NPC 名称翻译进度口径：只有普隆德拉达到全地图覆盖，其他核心城市改为部分覆盖。
- `[root]` 完成吉芬、梦罗克、斐扬、艾尔贝塔和依斯鲁得的第一阶段全地图 NPC 名称覆盖，并明确各城室内地图范围。
- `[client]` 将服务器显示名改为 `HappyRO`，并恢复进入游戏前的启动页。
- `[root]` 移除产品与部署文档中不必要的“局域网”标识。

## Fixed

- `[root]` 修复 Docker 网关依赖安装、游戏服 WebSocket 路由、kRO loose 资源挂载和服务端二进制复制路径；统一使用 `127.0.0.1` 本地逻辑地址映射内部服务，只对外暴露网关端口，统一 `kugarocks/happyro-*` 镜像和 `happyro-*` 容器名称，并限制服务端镜像编译并发以避免本地构建耗尽内存。
- `[root]` 将聊天栏默认标签“战役信息”校准为“战斗信息”。
- `[client]` 将专用弹窗中的韩文确定、取消、关闭、重置、购买和出售按钮统一汉化。
- `[client]` 补齐默认皮肤、银行、邮件、商城、逃生菜单及登录界面中仍可见的韩文或英文文字按钮。
- `[client]` 按像素解释 NPC 脚本中的 RO 字号标签，避免浏览器将强调文本渲染为超大字号。
- `[server]` 修复碳酸水自动售货机警告文本因 HTML 字号标签而显示为超大红字的问题。
- `[client]` 将登录等待框及系统消息框的韩文标题栏统一汉化为“提示”。
- `[client]` 让 PWA 完整构建以 roBrowser App Launcher 作为首页，并提供游戏及各查看器入口。
- `[client]` 汉化登录界面的“记住账号”选项和 `_M` / `_F` 快速注册说明。
- `[client]` 将告示牌、疯狂科学家和可疑男子改为跨地图适用的通用 NPC 译名。
- `[client]` 自动处理 24 字节名称封包造成的 23 字符英文名称截断，并检测截断别名冲突。
- `[client]` 移除未登记英文 NPC 统一显示为“城镇居民”的兜底，使名称覆盖检查能够报告真实缺失。
- `[client]` 在每页 NPC 对话到达时重新本地化标题，避免点击“继续”后名称恢复为英文。
- `[client]` 汉化魔物悬停名称及已登记的公共 NPC 名称。
- `[client]` 停用不完整的道具运行时覆盖层，改用完整的 `itemInfo_true.lub` 译文。
- `[server]` 保持“工匠学徒”多页对话标题为中文。
- `[server]` 恢复 36 个翻译过程中结构受损的 NPC 脚本，并通过 24,178 个 NPC 的 map-server 加载检查。
- `[server]` 缩短超过服务端字段上限的中文物品名和魔物名，消除数据库加载错误。
- `[server]` 为固定的 `PACKETVER=20211103` 停用不受支持的对话物品图标功能。
- `[root]` 初始化缺失的可选配置及数据库 import 模板，并保持已有本地配置不被覆盖。

## Documentation

- `[root]` 规范 NPC 名称翻译文档目录，合并城镇与非城镇计划和进度，并按区域归档批次清单。
- `[root]` 记录 NPC 初始实体名称、对话标题、内部后缀和名称封包截断的根因及验收方法。
- `[root]` 归档道具本地化覆盖数据和 NPC 名称维护规则。
- `[root]` 要求所有待提交变更在提交前同步添加或更新 changelog 记录。
