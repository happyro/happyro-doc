# 2026-08-27

## Fixed

- `[root]` 保存 RemoteClient-JS 混合编码资源路径修复，并在网关配置阶段自动应用全部根仓库补丁，避免重新克隆后物品和装备图片再次缺失。
- `[root]` 将网关修改迁移到独立的 HappyRO Gateway 仓库，移除根仓库补丁应用流程并锁定网关版本。
- `[root]` 更新 README，补充四个仓库的职责和项目组成说明。
- `[root]` 将 Gateway 依赖基线链接改为 RemoteClient-JS 上游仓库，并保留 HappyRO Gateway 锁定提交。
- `[root]` 将 Docker 运行所需的 SQL、初始化脚本和汉化覆盖文件打入镜像，预构建镜像部署只需 Compose 文件和 kRO 资源。
- `[root]` 将 Docker 部署目录统一为 `happyro`，并将数据库与服务日志持久化到 `data/db`、`data/gateway` 和 `data/server`。
- `[root]` 让服务端构建脚本在 macOS 没有 `nproc` 时回退到 `getconf`，保持 Linux、macOS 和 WSL2 的安装命令一致。
- `[root]` 修复数据库、网关、资源和服务端运维脚本的可执行位，确保 Makefile 目标可直接运行。

## Changed

- `[root]` 增加 Docker Hub 发布脚本，要求显式传入语义化版本并同时推送三个双架构镜像的版本标签和 `latest` 标签。
- `[root]` 将 rAthena 内部 inter-server 账号默认值改为 `happyro_interserver`，释放 `happyro` 作为玩家账号名称。
- `[root]` 记录全局活动 NPC 名称候选审计及完整 CSV 快照，并按全部实例地图和跨文件共享模板修正漏扫口径。
- `[root]` 更新 README，补充项目定位、基线、汉化流程、版权资源说明和 GNU GPL v3 协议。
- `[root]` 在 README 中增加 HappyRO 安装文档入口。
- `[root]` 将 README 中的项目基线和依赖基线拆分为独立章节。
